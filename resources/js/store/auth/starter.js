import ApiData from "@/api/server/modules/api.data";
import { TokenService } from "@/services";

export default {
  namespaced: true,
  state: {},
  mutations: {},
  getters: {},
  actions: {
    async checkingLicense({ dispatch, commit }, store) {
   
    },
  },
};
